# api 局部规约

本目录保存对外 API 契约工件。遵循根 AGENTS.md；本文件只补充契约文件的维护边界。

## 职责

- 维护当前已确认且已实现的 HTTP API 契约。
- 契约变更必须与对应 Handler、测试和运行行为保持一致。

## 边界与验证

- 只修改契约内容和必要的契约说明，不在此目录实现业务逻辑。
- 修改前确认 Method、Path、请求响应字段、状态码和错误语义。
- 最小验证：执行 go test -mod=readonly ./... -count=1，并检查契约文件格式。
