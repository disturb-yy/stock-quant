apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-quant.fullname" . }}
  labels:
    {{- include "stock-quant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stock-quant.selectorLabels" . | nindent 4 }}

